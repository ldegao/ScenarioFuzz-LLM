# 相似度打分方法对比实验运行指南

本文档详细说明如何运行相似度打分方法对比实验，对比四种不同的相似度打分方法在四个多样性指标上的表现。

## 目录

1. [实验概述](#实验概述)
2. [环境准备](#环境准备)
3. [运行实验](#运行实验)
4. [结果分析](#结果分析)
5. [常见问题](#常见问题)

---

## 实验概述

### 实验目标

对比四种相似度打分方法在四个多样性指标上的表现，以确定最佳的相似度打分方法：

- **answer2**: LLM基于相似度打分（当前默认方法）
- **embedding**: 基于embedding向量的语义相似度
- **feature**: 基于多维度特征（位置、速度、角加速度）的相似度
- **hybrid**: 混合embedding和特征相似度

### 评估指标

实验使用四个多样性指标来评估每种方法：

- **PC (Parameter Coverage)**: 参数空间组合覆盖率
- **PEC (Behavior Coverage)**: 物理行为等价类覆盖率
- **TCD (Trajectory Diversity)**: 轨迹模式多样性（使用DTW和熵）
- **BCM (Behavior Matrix)**: 行为组合覆盖率

### 实验输出

每个方法的实验结果会保存在独立目录中：
```
experiment_results/
  SimilarityComparison/
    SimilarityComparison_answer2_YYYYMMDD_HHMMSS/
      metrics_summary.json      # 指标汇总
      metrics_records.jsonl      # 每个场景的详细指标
      ...
    SimilarityComparison_embedding_YYYYMMDD_HHMMSS/
      ...
    SimilarityComparison_feature_YYYYMMDD_HHMMSS/
      ...
    SimilarityComparison_hybrid_YYYYMMDD_HHMMSS/
      ...
```

---

## 环境准备

### 1. 确保CARLA运行

在运行实验前，确保CARLA模拟器正在运行：

```bash
# 启动CARLA（如果还没有运行）
./run_carla.sh

# 检查CARLA是否运行
docker ps | grep carla
```

### 2. 激活Python虚拟环境

```bash
cd /path/to/ScenarioFuzz-LLM
source venv/bin/activate  # 或 source .venv/bin/activate
```

### 3. 检查依赖

确保已安装所有必要的依赖：

```bash
pip install -r requirements.txt
```

---

## 运行实验

### 方式一：批量运行所有方法（推荐）

这是最简单的方式，会自动依次运行所有四种方法：

```bash
bash experiments/scripts/run_similarity_comparison.sh \
  --num-scenarios 100 \
  --output-root ./experiment_results
```

**参数说明**：
- `--num-scenarios`: 每个方法生成的场景数量（建议：1000或更多）
- `--output-root`: 结果保存的根目录（默认：`./experiment_results`）
- `--target`: 目标ADS系统（`behavior` 或 `autoware`，默认：`behavior`）
- `--town`: CARLA城镇编号（默认：3）
- `--timeout`: 单个场景超时时间（秒，默认：60）
- `--rag-k`: RAG检索的top-k值（默认：5）
- `--hybrid-embedding-weight`: 混合方法中embedding的权重（默认：0.6）

**完整示例**：
```bash
bash experiments/scripts/run_similarity_comparison.sh \
  --num-scenarios 1000 \
  --output-root ./experiment_results \
  --target behavior \
  --town 3 \
  --timeout 60 \
  --rag-k 5 \
  --hybrid-embedding-weight 0.6
```

**运行时间估算**：
- 每个方法生成1000个场景大约需要：2-4小时（取决于硬件配置）
- 四个方法总共需要：8-16小时

### 方式二：单独运行每个方法

如果需要单独运行某个方法，或需要自定义参数：

#### 运行answer2方法（LLM-based）

```bash
python -m experiments.runners.run_similarity_comparison \
  --num-scenarios 1000 \
  --similarity-method answer2 \
  --output-root ./experiment_results \
  --target behavior \
  --town 3 \
  --timeout 60 \
  --rag-k 5
```

#### 运行embedding方法

```bash
python -m experiments.runners.run_similarity_comparison \
  --num-scenarios 1000 \
  --similarity-method embedding \
  --output-root ./experiment_results \
  --target behavior \
  --town 3 \
  --timeout 60 \
  --rag-k 5
```

#### 运行feature方法

```bash
python -m experiments.runners.run_similarity_comparison \
  --num-scenarios 1000 \
  --similarity-method feature \
  --output-root ./experiment_results \
  --target behavior \
  --town 3 \
  --timeout 60 \
  --rag-k 5 \
  --feature-position-weight 0.3 \
  --feature-speed-weight 0.3 \
  --feature-angular-accel-weight 0.2 \
  --feature-relative-position-weight 0.2
```

#### 运行hybrid方法

```bash
python -m experiments.runners.run_similarity_comparison \
  --num-scenarios 1000 \
  --similarity-method hybrid \
  --output-root ./experiment_results \
  --target behavior \
  --town 3 \
  --timeout 60 \
  --rag-k 5 \
  --hybrid-embedding-weight 0.6 \
  --feature-position-weight 0.3 \
  --feature-speed-weight 0.3 \
  --feature-angular-accel-weight 0.2 \
  --feature-relative-position-weight 0.2
```

### 参数详细说明

#### 必需参数

- `--num-scenarios`: 要生成的场景数量（整数，必须大于0）

- `--similarity-method`: 相似度打分方法
  - 可选值：`answer2`, `embedding`, `feature`, `hybrid`
  - 仅在单独运行时需要指定

#### 可选参数

- `--output-root`: 输出根目录（默认：`./experiment_results`）

- `--target`: 目标ADS系统
  - `behavior`: 行为模型（默认）
  - `autoware`: Autoware系统

- `--town`: CARLA城镇编号（默认：3）
  - 可选值：1, 2, 3, 4, 5等

- `--timeout`: 单个场景超时时间（秒，默认：60）

- `--rag-k`: RAG检索的top-k值（默认：5）
  - 控制从场景库中检索多少个相似场景

- `--hybrid-embedding-weight`: 混合方法中embedding的权重（默认：0.6）
  - 范围：0.0-1.0
  - feature权重 = 1.0 - embedding权重

- `--feature-position-weight`: 特征方法中位置相似度的权重（默认：0.3）

- `--feature-speed-weight`: 特征方法中速度相似度的权重（默认：0.3）

- `--feature-angular-accel-weight`: 特征方法中角加速度相似度的权重（默认：0.2）

- `--feature-relative-position-weight`: 特征方法中相对位置相似度的权重（默认：0.2）

- `--debug`: 启用调试模式（可选）

---

## 结果分析

### 1. 查看单个方法的指标

每个方法的实验结果目录中包含 `metrics_summary.json` 文件：

```bash
# 查看answer2方法的指标
cat ./experiment_results/SimilarityComparison/SimilarityComparison_answer2_*/metrics_summary.json | python -m json.tool
```

`metrics_summary.json` 包含：
```json
{
  "method": "SimilarityComparison",
  "experiment_id": "SimilarityComparison_answer2_20250101_120000",
  "num_scenarios": 1000,
  "num_records": 1000,
  "pc": 0.85,
  "pec": 0.72,
  "tcd": 0.68,
  "bcm": 0.79
}
```

### 2. 生成对比报告

运行所有方法后，生成对比报告：

```bash
python -m experiments.analysis.compare_similarity_methods \
  --results-dir ./experiment_results/SimilarityComparison \
  --output-dir ./reports/similarity_comparison
```

**输出文件**：
- `comparison_report.json`: JSON格式的详细对比数据
- `comparison_report.md`: Markdown格式的人类可读报告（包含表格）
- `comparison_figures/`: 可视化图表目录
  - `pc_comparison.png`: PC指标对比柱状图
  - `pec_comparison.png`: PEC指标对比柱状图
  - `tcd_comparison.png`: TCD指标对比柱状图
  - `bcm_comparison.png`: BCM指标对比柱状图
  - `radar_comparison.png`: 四个指标的综合雷达图

### 3. 查看对比报告

```bash
# 查看Markdown报告
cat ./reports/similarity_comparison/comparison_report.md

# 查看JSON报告
cat ./reports/similarity_comparison/comparison_report.json | python -m json.tool

# 查看图表（需要图片查看器）
# 图表保存在 ./reports/similarity_comparison/comparison_figures/
```

### 4. 报告内容说明

对比报告包含：

1. **方法对比表格**：显示每个方法在四个指标上的平均值和标准差
2. **详细统计信息**：每个方法的每个指标的均值、标准差、最小值、最大值和运行次数
3. **可视化图表**：
   - 柱状图：直观对比每个指标
   - 雷达图：综合对比所有指标（归一化后）

---

## 常见问题

### Q1: 实验运行时间太长怎么办？

**A**: 可以：
1. 减少场景数量（如从1000改为500）
2. 只运行部分方法进行初步测试
3. 使用更快的硬件配置
4. 在后台运行实验（使用 `nohup` 或 `screen`/`tmux`）

```bash
# 使用nohup在后台运行
nohup bash experiments/scripts/run_similarity_comparison.sh \
  --num-scenarios 1000 \
  --output-root ./experiment_results > experiment.log 2>&1 &

# 查看运行日志
tail -f experiment.log
```

### Q2: CARLA连接失败怎么办？

**A**: 
1. 检查CARLA是否运行：`docker ps | grep carla`
2. 如果CARLA未运行，启动它：`./run_carla.sh`
3. 检查端口是否被占用：`netstat -tuln | grep 2000`
4. 重启CARLA容器：`docker restart carla-${USER}`

### Q3: 实验中断了怎么办？

**A**: 实验支持断点续传（如果启用了checkpoint）：
1. 检查是否有checkpoint文件：`ls ./experiment_results/SimilarityComparison/*/ga_checkpoint.pkl`
2. 如果有checkpoint，可以继续实验（需要修改代码支持继续功能）
3. 如果没有checkpoint，需要重新运行

### Q4: 内存不足怎么办？

**A**: 
1. 减少场景数量
2. 关闭其他占用内存的程序
3. 使用更小的RAG k值（如从5改为3）
4. 定期清理临时文件

### Q5: 如何只运行部分方法？

**A**: 使用单独运行方式，只运行需要的方法：

```bash
# 只运行embedding和hybrid方法
python -m experiments.runners.run_similarity_comparison \
  --num-scenarios 1000 \
  --similarity-method embedding \
  --output-root ./experiment_results

python -m experiments.runners.run_similarity_comparison \
  --num-scenarios 1000 \
  --similarity-method hybrid \
  --output-root ./experiment_results
```

### Q6: 如何调整相似度方法的参数？

**A**: 使用相应的参数：

```bash
# 调整hybrid方法的embedding权重
python -m experiments.runners.run_similarity_comparison \
  --num-scenarios 1000 \
  --similarity-method hybrid \
  --hybrid-embedding-weight 0.7 \  # 增加embedding权重
  --output-root ./experiment_results

# 调整feature方法的权重
python -m experiments.runners.run_similarity_comparison \
  --num-scenarios 1000 \
  --similarity-method feature \
  --feature-position-weight 0.4 \  # 增加位置权重
  --feature-speed-weight 0.3 \
  --feature-angular-accel-weight 0.2 \
  --feature-relative-position-weight 0.1 \
  --output-root ./experiment_results
```

### Q7: 如何查看实验进度？

**A**: 
1. 查看输出目录中的场景文件数量：
   ```bash
   ls ./experiment_results/SimilarityComparison/SimilarityComparison_*/queue/*.json | wc -l
   ```

2. 查看最新的metrics记录：
   ```bash
   tail -f ./experiment_results/SimilarityComparison/SimilarityComparison_*/metrics_records.jsonl
   ```

3. 查看实验日志（如果有）：
   ```bash
   tail -f experiment.log
   ```

### Q8: 结果文件在哪里？

**A**: 结果文件结构：
```
experiment_results/
  SimilarityComparison/
    SimilarityComparison_answer2_YYYYMMDD_HHMMSS/
      metrics_summary.json          # 指标汇总
      metrics_records.jsonl         # 每个场景的详细指标
      queue/                         # 生成的场景JSON文件
        gid:00001_sid:00001.json
        ...
      ga_checkpoint.pkl             # GA状态检查点（如果启用）
      ...
```

---

## 实验建议

### 1. 场景数量建议

- **快速测试**：100-200个场景（约30分钟-1小时）
- **初步对比**：500个场景（约1-2小时）
- **完整实验**：1000个场景（约2-4小时）
- **论文实验**：2000+个场景（约4-8小时）

### 2. 运行顺序建议

建议按以下顺序运行，以便及时发现和解决问题：

1. 先运行一个方法（如answer2）进行测试
2. 确认结果正确后，再批量运行所有方法
3. 如果某个方法失败，可以单独重新运行该方法

### 3. 参数调优建议

- **RAG k值**：通常5-10效果较好，太大可能引入噪声
- **Hybrid权重**：建议从0.6开始，根据结果调整
- **Feature权重**：默认权重已经过调优，一般不需要修改

### 4. 结果解读建议

- **关注平均值**：主要看每个方法的平均指标值
- **关注标准差**：标准差小说明结果稳定
- **综合对比**：不要只看单个指标，要综合四个指标判断
- **多次运行**：如果可能，建议每个方法运行多次取平均

---

## 快速开始示例

### 完整实验流程（推荐）

```bash
# 1. 确保环境准备就绪
source venv/bin/activate
./run_carla.sh  # 如果CARLA未运行

# 2. 批量运行所有方法（1000个场景）
bash experiments/scripts/run_similarity_comparison.sh \
  --num-scenarios 1000 \
  --output-root ./experiment_results

# 3. 等待所有方法运行完成（可能需要8-16小时）

# 4. 生成对比报告
python -m experiments.analysis.compare_similarity_methods \
  --results-dir ./experiment_results/SimilarityComparison \
  --output-dir ./reports/similarity_comparison

# 5. 查看结果
cat ./reports/similarity_comparison/comparison_report.md
```

### 快速测试流程（100个场景）

```bash
# 1. 只运行embedding方法进行快速测试
python -m experiments.runners.run_similarity_comparison \
  --num-scenarios 100 \
  --similarity-method embedding \
  --output-root ./experiment_results

# 2. 查看结果
cat ./experiment_results/SimilarityComparison/SimilarityComparison_embedding_*/metrics_summary.json | python -m json.tool
```

---

## 联系与支持

如果遇到问题，请：
1. 查看本文档的常见问题部分
2. 检查实验日志文件
3. 查看主README.md获取更多信息
4. 检查 `experiments/docs/` 目录下的其他文档

---

**最后更新**: 2025-01-XX

