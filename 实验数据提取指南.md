# 实验数据提取指南

## 1. 如何获取论文中需要的实验数据？

运行 `python -m experiments.runners.run_scenariofuzz_llm --num-scenarios 100 --output-root ./experiment_results` 后，实验数据存储在以下位置：

### 1.1 单个实验的数据位置

实验数据存储在：
```
./experiment_results/ScenarioFuzz-LLM/<experiment_id>/
```

例如您最近的实验：
```
./experiment_results/ScenarioFuzz-LLM/ScenarioFuzz-LLM_20251204_210752/
```

### 1.2 关键数据文件

每个实验目录包含以下重要文件：

#### **metrics/metrics_summary.json** - 实验汇总指标
包含以下论文中需要的指标：
- `pc`: Parameter Coverage (参数覆盖率)
- `pec`: Physical behavior Equivalence Class coverage (行为等价类覆盖率)
- `tcd`: Trajectory Diversity (轨迹多样性)
- `bcm`: Behavior Combination Matrix (行为组合矩阵)
- `num_scenarios`: 生成的场景数量
- `num_records`: 记录的场景数量

#### **metrics/metrics_records.jsonl** - 每个场景的详细记录
每行一个JSON对象，包含每个场景的详细指标：
```json
{"generation_id": 2, "scenario_id": 111, "pc": 7.407407407407407e-05, "pec": 0.1, "tcd": 0.0, "bcm": 0.000244140625}
```

#### **token_usage.json** - Token使用统计
包含GPT API调用的token使用情况（详见第3节）

#### **其他数据**
- `time_record/`: 每个场景的时间序列数据
- `camera/`: 场景视频记录
- `picture/`: 场景截图
- `ga_checkpoint.pkl`: 遗传算法检查点

### 1.3 聚合所有实验数据（用于论文对比）

如果您运行了多个实验（例如对比不同方法），需要聚合数据：

```bash
# 聚合所有实验的metrics数据
python -m experiments.aggregation.main
```

这会生成：
- `./experiment_results/all_methods_results.json` - 包含所有方法和实验的汇总数据

### 1.4 生成论文图表和报告

```bash
# 生成图表（PC柱状图、多指标雷达图等）
python -m experiments.analysis.generate_figures \
  --results-file ./experiment_results/all_methods_results.json \
  --output-dir ./reports/figs

# 生成Markdown和JSON报告
python -m experiments.analysis.generate_reports \
  --results-file ./experiment_results/all_methods_results.json \
  --output-dir ./reports \
  --experiment-name Thesis_Experiment
```

或者一次性生成：
```bash
python -m experiments.analysis.main
```

## 2. 运行结果看起来正确吗？

根据您提供的输出，实验运行**基本正确**：

✅ **正确的部分：**
- 成功生成了104个场景（超过目标的100个）
- 每个场景都进行了评估（显示了相似度、车辆信息等）
- 检测到了碰撞（`COLLISION: vehicle.lincoln.mkz_2020`）
- 检测到了车道入侵（`LANE INVASION`）
- 检查点保存成功（`[INFO] Checkpoint saved`）
- Token使用统计已输出

⚠️ **需要注意的部分：**
- 实验生成了104个场景，但目标只有100个。这是因为遗传算法可能会在达到目标后继续完成当前代。
- 有些场景超时（`timeout after 15 min`），这是正常的，说明这些场景没有触发错误。
- 有些场景触发了错误（`fuzzer - found an error`），这是预期的，说明fuzzer正在寻找缺陷。

## 3. GPT Token记录情况

✅ **Token记录功能已经存在并正常工作！**

### 3.1 Token记录位置

Token使用情况已自动保存到：
```
./experiment_results/ScenarioFuzz-LLM/<experiment_id>/token_usage.json
```

### 3.2 Token记录内容

根据您的实验，`token_usage.json` 文件包含：

```json
{
  "overall": {
    "prompt_tokens": 921384,
    "completion_tokens": 36848,
    "total_tokens": 958232,
    "call_count": 106
  },
  "by_model": {
    "gpt-4-turbo": {
      "prompt_tokens": 921384,
      "completion_tokens": 36848,
      "total_tokens": 958232,
      "call_count": 106
    }
  }
}
```

### 3.3 Token记录功能说明

- **自动记录**：每次调用GPT API时，token使用情况会自动记录
- **实时统计**：实验结束时会在控制台打印Token使用摘要
- **文件保存**：自动保存到实验目录的 `token_usage.json` 文件
- **按模型分类**：分别统计不同模型（如gpt-4-turbo）的使用情况

### 3.4 查看Token使用情况

您可以通过以下方式查看：

1. **直接查看JSON文件**：
```bash
cat ./experiment_results/ScenarioFuzz-LLM/ScenarioFuzz-LLM_20251204_210752/token_usage.json
```

2. **在Python中读取**：
```python
import json
with open('./experiment_results/ScenarioFuzz-LLM/ScenarioFuzz-LLM_20251204_210752/token_usage.json', 'r') as f:
    token_data = json.load(f)
    print(f"总Token数: {token_data['overall']['total_tokens']:,}")
    print(f"API调用次数: {token_data['overall']['call_count']}")
```

## 4. 快速数据提取脚本示例

如果您需要快速提取特定数据，可以使用以下Python脚本：

```python
import json
from pathlib import Path

# 实验目录
exp_dir = Path("./experiment_results/ScenarioFuzz-LLM/ScenarioFuzz-LLM_20251204_210752")

# 读取指标汇总
with open(exp_dir / "metrics/metrics_summary.json") as f:
    metrics = json.load(f)
    print("实验指标:")
    print(f"  PC: {metrics['pc']}")
    print(f"  PEC: {metrics['pec']}")
    print(f"  TCD: {metrics['tcd']}")
    print(f"  BCM: {metrics['bcm']}")
    print(f"  场景数: {metrics['num_scenarios']}")

# 读取Token使用
with open(exp_dir / "token_usage.json") as f:
    tokens = json.load(f)
    print("\nToken使用:")
    print(f"  总Token: {tokens['overall']['total_tokens']:,}")
    print(f"  API调用: {tokens['overall']['call_count']}")
```

## 5. 关于实验目录和归档的说明

### 5.1 实验目录管理

**重要**：每次运行命令时，系统会：

1. **创建唯一的实验目录**：
   - 如果未指定 `--experiment-id`，会自动生成：`ScenarioFuzz-LLM_YYYYMMDD_HHMMSS`
   - 如果指定了 `--experiment-id`，使用指定的ID
   - **同一个experiment_id只会有一个实验目录**

2. **数据存储位置**：
   - 所有实验数据保存在：`./experiment_results/ScenarioFuzz-LLM/<experiment_id>/`
   - `token_usage.json` 保存在实验目录根目录
   - 每次运行只会更新同一个目录，不会创建多个目录

3. **如果实验目录已存在**：
   - 系统会检测是否有旧数据
   - 如果有旧数据，会清除旧数据并重新开始
   - 这确保每次运行都是干净的开始

### 5.2 归档目录（可选）

实验完成后，系统会创建一个归档快照（用于备份和可重现性）：

- **归档位置**：`./data/experiment_snapshots/ScenarioFuzz-LLM/<experiment_id>/`
- **重要改进**：现在使用 `experiment_id` 作为归档目录名（不再添加时间戳）
- **结果**：每个实验只有一个归档目录，不会创建多个

**注意**：
- 归档只在实验成功生成场景后执行
- 如果实验失败或中断，不会创建归档
- 如果归档目录已存在，会被更新（覆盖）

### 5.3 确保使用同一个实验目录

如果您想确保多次运行使用同一个实验目录，可以：

```bash
# 方式1：指定experiment_id
python -m experiments.runners.run_scenariofuzz_llm \
  --num-scenarios 100 \
  --experiment-id my_experiment_001 \
  --output-root ./experiment_results

# 方式2：继续已有实验（推荐）
python -m experiments.runners.run_scenariofuzz_llm \
  --continue-experiment ScenarioFuzz-LLM_20251204_210752 \
  --continue-scenarios 50 \
  --output-root ./experiment_results
```

## 总结

1. ✅ **实验数据**：存储在 `./experiment_results/ScenarioFuzz-LLM/<experiment_id>/` 目录
2. ✅ **关键指标**：在 `metrics/metrics_summary.json` 和 `metrics/metrics_records.jsonl` 中
3. ✅ **Token记录**：已自动记录在 `token_usage.json` 中
4. ✅ **实验运行**：看起来正常，成功生成了104个场景
5. ✅ **目录管理**：每次运行只有一个实验目录和一个token_usage.json文件
6. ✅ **归档优化**：每个实验只有一个归档目录，不会重复创建

您可以直接使用这些数据来撰写论文！

