# 实验方法对比说明

## 概述

根据代码和文档，当前系统支持**三个主要的实验方法**进行对比：

1. **ScenarioFuzz-LLM** - 使用LLM引导的遗传算法
2. **RAG-ScenarioFuzz** - 使用RAG增强的ScenarioFuzz
3. **TM-Fuzzer** - 基线方法（传统遗传算法）

## 三个方法的详细对比

### 1. ScenarioFuzz-LLM

**核心特点**：
- 使用**大语言模型（LLM）**作为专家代理来引导变异
- 当遗传算法遇到停滞时，使用LLM生成新的变异策略
- 增强场景多样性，突破多样性瓶颈

**技术实现**：
- 基于遗传算法（GA）
- 集成GPT-4-turbo进行场景分析和变异指导
- 使用多目标优化评估场景

**运行命令**：
```bash
python -m experiments.runners.run_scenariofuzz_llm \
  --num-scenarios 1000 \
  --output-root ./experiment_results
```

**适用场景**：
- 需要高多样性的场景生成
- 突破传统遗传算法的局部最优
- 发现新的边缘案例

**评估指标**：
- PC (Parameter Coverage) - 参数覆盖率
- PEC (Behavior Coverage) - 行为等价类覆盖率
- TCD (Trajectory Diversity) - 轨迹多样性
- BCM (Behavior Matrix) - 行为组合矩阵

---

### 2. RAG-ScenarioFuzz

**核心特点**：
- 在ScenarioFuzz-LLM基础上增加**RAG（检索增强生成）**模块
- 使用语义搜索从知识库中检索相关场景
- 提供上下文感知的场景生成提示
- 通过知识引导的变异提高场景多样性

**技术实现**：
- 基于ScenarioFuzz-LLM
- 增加RAG检索模块（`rag_module/`）
- 使用语义搜索检索相似场景
- 增强的提示生成（包含检索到的场景上下文）

**运行命令**：
```bash
python -m experiments.runners.run_rag_scenariofuzz \
  --num-scenarios 1000 \
  --rag-k 5 \
  --output-root ./experiment_results
```

**特有参数**：
- `--rag-k INT`：RAG检索的top-k数量（默认5）

**适用场景**：
- 需要利用历史场景知识
- 需要语义相似性搜索
- 需要上下文感知的场景生成

**评估指标**：
- 与ScenarioFuzz-LLM相同的四个指标（PC, PEC, TCD, BCM）

---

### 3. TM-Fuzzer（基线方法）

**核心特点**：
- **传统遗传算法**基线方法
- 不使用LLM或RAG
- 纯基于遗传算法的场景生成
- 作为对比基线使用

**技术实现**：
- 基于传统遗传算法
- 不依赖GPT API
- 使用`script/test.py`作为底层实现
- 支持Autoware和Behavior两种目标

**运行命令**：
```bash
python -m experiments.runners.run_tmfuzzer \
  --num-scenarios 1000 \
  --target autoware \
  --output-root ./experiment_results
```

**适用场景**：
- 作为基线对比方法
- 不需要LLM能力的场景
- 传统遗传算法性能评估

**评估指标**：
- 与ScenarioFuzz-LLM相同的四个指标（PC, PEC, TCD, BCM）

---

## 三个方法的对比维度

### 技术栈对比

| 方法 | LLM | RAG | 遗传算法 | GPT API依赖 |
|------|-----|-----|---------|------------|
| **ScenarioFuzz-LLM** | ✅ | ❌ | ✅ | ✅ |
| **RAG-ScenarioFuzz** | ✅ | ✅ | ✅ | ✅ |
| **TM-Fuzzer** | ❌ | ❌ | ✅ | ❌ |

### 功能特性对比

| 特性 | ScenarioFuzz-LLM | RAG-ScenarioFuzz | TM-Fuzzer |
|------|-------------------|------------------|-----------|
| **LLM引导变异** | ✅ | ✅ | ❌ |
| **RAG语义检索** | ❌ | ✅ | ❌ |
| **知识库利用** | ❌ | ✅ | ❌ |
| **上下文感知** | 部分 | ✅ | ❌ |
| **Token使用** | 高 | 高 | 无 |
| **运行成本** | 高 | 高 | 低 |

### 性能预期对比

根据论文描述：

| 指标 | ScenarioFuzz-LLM | RAG-ScenarioFuzz | TM-Fuzzer |
|------|-------------------|------------------|-----------|
| **场景多样性** | 高（+35.62%） | 更高 | 基线 |
| **缺陷发现** | 24个独特缺陷 | 预期更高 | 基线 |
| **参数覆盖率(PC)** | 高 | 更高 | 基线 |
| **行为覆盖率(PEC)** | 高 | 更高 | 基线 |
| **轨迹多样性(TCD)** | 高 | 更高 | 基线 |
| **行为组合(BCM)** | 高 | 更高 | 基线 |

---

## 四个评估指标说明

所有三个方法都使用相同的**四个评估指标**进行对比：

### 1. PC (Parameter Coverage) - 参数覆盖率
- **定义**：参数空间组合覆盖率
- **作用**：衡量测试场景覆盖了多少参数组合
- **计算**：基于参数空间的组合覆盖情况

### 2. PEC (Physical behavior Equivalence Class coverage) - 行为等价类覆盖率
- **定义**：物理行为等价类覆盖率
- **作用**：衡量测试场景覆盖了多少不同的行为等价类
- **计算**：基于物理行为的等价类划分

### 3. TCD (Trajectory Diversity) - 轨迹多样性
- **定义**：轨迹模式多样性
- **作用**：衡量生成的轨迹模式的多样性
- **计算**：使用DTW（动态时间规整）和熵计算

### 4. BCM (Behavior Combination Matrix) - 行为组合矩阵
- **定义**：行为组合覆盖率
- **作用**：衡量测试场景覆盖了多少行为组合
- **计算**：基于行为组合矩阵的覆盖情况

---

## 实验运行流程

### 1. 运行三个方法

```bash
# 方法1：ScenarioFuzz-LLM
python -m experiments.runners.run_scenariofuzz_llm \
  --num-scenarios 1000 \
  --output-root ./experiment_results

# 方法2：RAG-ScenarioFuzz
python -m experiments.runners.run_rag_scenariofuzz \
  --num-scenarios 1000 \
  --rag-k 5 \
  --output-root ./experiment_results

# 方法3：TM-Fuzzer（基线）
python -m experiments.runners.run_tmfuzzer \
  --num-scenarios 1000 \
  --target autoware \
  --output-root ./experiment_results
```

### 2. 聚合指标

```bash
python -m experiments.aggregation.main
```

生成：`./experiment_results/all_methods_results.json`

### 3. 生成对比图表和报告

```bash
python -m experiments.analysis.main
```

生成：
- `./reports/figs/pc_coverage.png` - PC对比柱状图
- `./reports/figs/metrics_radar.png` - 多指标雷达图
- `./reports/*.md` - Markdown报告
- `./reports/*.json` - JSON总结

---

## 常见问题

### Q1: 为什么只有三个方法？

A: 根据代码和文档，系统实现了三个主要方法：
- ScenarioFuzz-LLM（LLM引导）
- RAG-ScenarioFuzz（RAG增强）
- TM-Fuzzer（基线）

### Q2: 如果提到"四个实验"，可能是指什么？

A: 可能是指：
1. **四个评估指标**（PC, PEC, TCD, BCM）
2. **不同的配置组合**（如不同的target：behavior vs autoware）
3. **不同的实验设置**（定量 vs 定时）

### Q3: 如何选择使用哪个方法？

A: 
- **ScenarioFuzz-LLM**：需要LLM引导但不需要RAG时
- **RAG-ScenarioFuzz**：需要利用历史场景知识和语义检索时
- **TM-Fuzzer**：作为基线对比，或不需要LLM时

### Q4: 三个方法的Token使用情况如何？

A:
- **ScenarioFuzz-LLM**：每次场景生成需要调用GPT API
- **RAG-ScenarioFuzz**：每次场景生成需要调用GPT API（可能更多，因为包含RAG检索）
- **TM-Fuzzer**：不使用GPT API，无Token消耗

---

## 总结

当前系统支持**三个实验方法**的对比：
1. **ScenarioFuzz-LLM** - LLM引导的遗传算法
2. **RAG-ScenarioFuzz** - RAG增强的ScenarioFuzz
3. **TM-Fuzzer** - 传统遗传算法基线

所有方法都使用**四个评估指标**（PC, PEC, TCD, BCM）进行评估和对比。

如果您提到的"四个实验"是指其他内容，请告诉我具体是什么，我可以进一步说明。

