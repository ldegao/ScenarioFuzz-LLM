# 修复说明：实验目录和归档管理

## 问题描述

之前存在的问题：
1. 每次实验结束（包括中断）都会创建归档目录，且使用时间戳命名
2. 导致同一个实验可能创建多个归档目录：`experiment_id_20251204_210752`, `experiment_id_20251205_020834` 等
3. 用户希望每次运行只有一个实验目录和一个token_usage.json文件

## 修复内容

### 1. 归档目录命名优化

**修改前**：
```python
archive_dir = archive_root / f"{experiment_id}_{timestamp}"
# 每次归档都创建新目录，如：ScenarioFuzz-LLM_20251204_210752_20251205_020834
```

**修改后**：
```python
archive_dir = archive_root / experiment_id
# 使用experiment_id作为目录名，如：ScenarioFuzz-LLM_20251204_210752
# 如果已存在，会先删除再创建（确保最新数据）
```

**文件位置**：`experiments/core/experiment_manager.py` 的 `_archive_experiment_run` 方法

### 2. 归档条件优化

**修改前**：
- 在 `finally` 块中无条件归档
- 即使实验失败或中断也会归档

**修改后**：
- 只在实验成功生成场景后归档
- 检查场景数量，只有 `actual_count > 0` 时才归档
- 避免为失败的实验创建归档

**修改位置**：
- `run_quantitative_experiment` 的 `finally` 块
- `run_timed_experiment` 的 `finally` 块
- `continue_experiment` 的 `finally` 块

### 3. 实验目录管理

实验目录的管理逻辑保持不变（已经正确）：
- 如果 `experiment_id` 已存在且有数据，会清除旧数据重新开始
- 确保每次运行使用同一个实验目录
- `token_usage.json` 保存在实验目录根目录，每次运行会更新

## 修复效果

### 修复前的问题
```
运行一次命令（可能中断多次）：
- 实验目录：./experiment_results/ScenarioFuzz-LLM/ScenarioFuzz-LLM_20251204_210752/
- 归档目录1：./data/experiment_snapshots/.../ScenarioFuzz-LLM_20251204_210752_20251205_020834/
- 归档目录2：./data/experiment_snapshots/.../ScenarioFuzz-LLM_20251204_210752_20251205_021045/
- 归档目录3：./data/experiment_snapshots/.../ScenarioFuzz-LLM_20251204_210752_20251205_021156/
```

### 修复后的效果
```
运行一次命令（可能中断多次）：
- 实验目录：./experiment_results/ScenarioFuzz-LLM/ScenarioFuzz-LLM_20251204_210752/
  - token_usage.json（只有一个，每次更新）
  - metrics/metrics_summary.json
  - metrics/metrics_records.jsonl
  - ...
- 归档目录：./data/experiment_snapshots/.../ScenarioFuzz-LLM_20251204_210752/（只有一个）
```

## 使用建议

### 1. 确保使用同一个实验目录

**方式1：使用指定的experiment_id**
```bash
python -m experiments.runners.run_scenariofuzz_llm \
  --num-scenarios 100 \
  --experiment-id my_experiment_001 \
  --output-root ./experiment_results
```

**方式2：继续已有实验（推荐）**
```bash
python -m experiments.runners.run_scenariofuzz_llm \
  --continue-experiment ScenarioFuzz-LLM_20251204_210752 \
  --continue-scenarios 50 \
  --output-root ./experiment_results
```

### 2. 查看实验数据

每次运行后，数据都在同一个目录：
```bash
# 查看token使用情况
cat ./experiment_results/ScenarioFuzz-LLM/<experiment_id>/token_usage.json

# 查看指标汇总
cat ./experiment_results/ScenarioFuzz-LLM/<experiment_id>/metrics/metrics_summary.json

# 查看所有场景记录
cat ./experiment_results/ScenarioFuzz-LLM/<experiment_id>/metrics/metrics_records.jsonl
```

### 3. 归档说明

- 归档是**可选的备份功能**，不影响主要实验数据
- 归档只在实验成功完成后创建
- 每个实验只有一个归档目录
- 如果需要禁用归档，可以修改代码中的归档调用

## 技术细节

### 修改的文件

1. `experiments/core/experiment_manager.py`
   - `_archive_experiment_run` 方法：修改归档目录命名
   - `run_quantitative_experiment` 方法：添加归档条件检查
   - `run_timed_experiment` 方法：添加归档条件检查
   - `continue_experiment` 方法：添加归档条件检查

### 关键代码变更

```python
# 归档目录命名（修改后）
archive_dir = archive_root / experiment_id  # 不再添加时间戳

# 归档条件检查（新增）
if method_dir.exists():
    actual_count = self._count_scenarios(method_dir)
    if actual_count > 0:  # 只有成功生成场景才归档
        self._archive_experiment_run(method_name, experiment_id, method_dir)
    else:
        print(f"[INFO] Skipping archive: no scenarios generated")
```

## 验证

修复后，您可以验证：

1. **运行一次实验**：
   ```bash
   python -m experiments.runners.run_scenariofuzz_llm --num-scenarios 100
   ```

2. **检查实验目录**：
   - 应该只有一个实验目录：`./experiment_results/ScenarioFuzz-LLM/<experiment_id>/`
   - 应该只有一个 `token_usage.json` 文件

3. **检查归档目录**（如果实验成功）：
   - 应该只有一个归档目录：`./data/experiment_snapshots/ScenarioFuzz-LLM/<experiment_id>/`
   - 不会因为多次中断而创建多个归档目录

## 总结

✅ **修复完成**：
- 每个实验只有一个实验目录
- 每个实验只有一个 `token_usage.json` 文件
- 每个实验只有一个归档目录（如果成功完成）
- 归档只在实验成功生成场景后执行

现在您可以放心使用，每次运行命令只会有一个实验目录和对应的JSON文件！

