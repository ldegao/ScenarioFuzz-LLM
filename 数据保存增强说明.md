# 数据保存增强说明

## 概述

本次更新增强了实验过程中的数据保存功能，确保在实验运行后能够保存尽可能多的过程信息，避免后续还需要重新分析。

## 主要改进

### 1. 大模型更换

- **原模型**: `gpt-4-turbo`
- **新模型**: `gpt-5-mini`
- **修改位置**:
  - `gpt.py`: `call_gpt()` 和 `call_gpt_with_rag()` 函数的默认参数
  - `fuzzer.py`: GPT 调用处
  - `experiments/core/token_tracker.py`: 默认模型名称

### 2. 增强的 GPT 日志保存

在 `fuzzer.py` 的 `evaluation()` 函数中，GPT 对话日志现在包含以下增强信息：

#### 基础标识信息
- `generation_id`: 代数ID
- `scenario_id`: 场景ID
- `timestamp`: 时间戳（格式：YYYYMMDD_HHMMSS）
- `iso_timestamp`: ISO 格式时间戳（更精确）

#### GPT 调用元数据
- `gpt_call_duration_seconds`: GPT API 调用耗时（秒）
- `model_version`: 使用的模型版本（gpt-5-mini）
- `max_tokens`: 最大 token 数

#### 请求/响应数据
- `prompt`: 完整的提示词
- `prompt_length`: 提示词长度
- `raw_response`: 原始响应文本
- `response_length`: 响应文本长度
- `parsed_response`: 解析后的 JSON 响应

#### Token 使用统计
- `token_stats`: 累积的 token 使用统计
  - `cumulative_prompt_tokens`: 累积的提示词 token 数
  - `cumulative_completion_tokens`: 累积的完成 token 数
  - `cumulative_total_tokens`: 累积的总 token 数
  - `cumulative_call_count`: 累积的调用次数

#### 场景上下文信息
- `scenario_description`: 场景描述
- `scenario_state`: 场景状态信息
  - `min_dist`: 最小距离
  - `min_dist_frame`: 最小距离帧
  - `fitness`: 适应度值
  - `fitness_values`: 适应度值详情

#### RAG 信息（如果启用）
- `rag_info`: RAG 相关配置和状态
  - `rag_enabled`: 是否启用 RAG
  - `rag_k`: RAG 检索的 top-k 值
  - `retrieved_scenarios_count`: 检索到的场景数量
  - `use_enhanced_rag`: 是否使用增强 RAG
  - `use_hybrid_search`: 是否使用混合搜索
  - `use_reranking`: 是否使用重排序

#### 场景数据库信息
- `scenario_database_size`: 场景数据库大小

#### 评估结果
- `overall_similarity`: 总体相似度分数
- `answer3_vehicle_info`: 答案3中的车辆信息

### 3. GPT API 调用增强

在 `gpt.py` 的 `call_gpt()` 函数中：
- 记录请求开始时间
- 记录请求结束时间和耗时
- 记录响应元数据（response_id, model, created, finish_reason）
- 打印请求耗时信息

### 4. 数据保存位置

所有增强的日志数据保存在：
- **GPT 对话日志**: `./data/gpt_logs/` 目录
- **Token 使用统计**: 实验目录下的 `token_usage.json`
- **实验快照**: `./data/experiment_snapshots/` 目录

## 使用建议

### 分析实验数据

实验运行后，可以通过以下方式分析保存的数据：

1. **GPT 对话日志分析**:
   ```python
   import json
   import glob
   
   # 读取所有 GPT 日志
   log_files = glob.glob("./data/gpt_logs/*.json")
   for log_file in log_files:
       with open(log_file, 'r', encoding='utf-8') as f:
           log_data = json.load(f)
           # 分析 log_data 中的各种字段
   ```

2. **Token 使用分析**:
   ```python
   import json
   
   with open("./experiment_results/ScenarioFuzz-LLM/<experiment_id>/token_usage.json", 'r') as f:
       token_stats = json.load(f)
       # 分析 token 使用情况
   ```

3. **实验快照分析**:
   实验完成后，所有相关数据会自动归档到 `./data/experiment_snapshots/` 目录，包括：
   - 实验结果
   - 场景数据库
   - GPT 日志
   - Token 统计
   - 进度检查点

## 测试

已创建测试脚本 `test_gpt_connectivity.py` 用于测试：
- GPT-4o-mini 连通性
- Token 追踪功能

运行测试：
```bash
python3 test_gpt_connectivity.py
```

## 注意事项

1. 所有时间戳使用 ISO 格式，便于跨时区分析
2. Token 统计是累积的，反映整个实验过程中的使用情况
3. RAG 相关信息仅在启用 RAG 时记录
4. 某些字段可能为 None（如果获取失败或未启用相关功能）

## 后续分析建议

基于保存的丰富数据，可以进行以下分析：

1. **性能分析**: 通过 `gpt_call_duration_seconds` 分析 API 响应时间
2. **成本分析**: 通过 `token_stats` 分析 token 使用和成本
3. **质量分析**: 通过 `overall_similarity` 和 `fitness_values` 分析场景质量
4. **RAG 效果分析**: 通过 `rag_info` 分析 RAG 检索效果
5. **时间序列分析**: 通过 `iso_timestamp` 分析实验过程的时间特征

