# 自动驾驶行为覆盖指标体系（归档版）

## 1 指标体系概述
在自动驾驶复杂行为空间中，行为组合呈指数级增长，使得线性覆盖率指标在实际评测中通常趋近于 0，导致区分度不足。本研究采用对数归一化与稳定化熵归一化方法，以提高指标的可解释性和可比较性。

---

## 2 行为参数配置熵 PCE  
### 2.1 定义（Hartley 熵）
离散化后的行为参数网格理论组合数为 M，实验实际覆盖组合数为 x。  
```
PCE = log(x) / log(M)
```
当 x=0 时定义为 0。

---

## 3 行为组合覆盖熵 CCE  
### 3.1 定义（Hartley 熵）  
行为集合 B 的理论组合数：T_max = 2^{|B|}，实验覆盖组合数为 T。  
```
CCE = log(T) / log(T_max)
```
当 T=0 时定义为 0。

---

## 4 驾驶模式熵 DPE  
基于 Shannon 熵的轨迹聚类多样性：  
```
H = -Σ p_k log p_k
H_max = log K
DPE = H / log K
```
聚类距离可用 Fréchet / DTW，K>1 时定义有效。

---

## 5 行为类别熵 BCE  
基于 Shannon 熵的行为类别分布：  
```
H = -Σ p_c log p_c
H_max = log M
BCE = H / log M
```
其中 p_c 由各场景出现频次估计。

---

## 6 四指标总结  
| 指标 | 目的 | 特性 | 归一化 |
|------|------|------|--------|
| PCE | 行为参数配置 | 稀疏大空间 | Hartley 熵 |
| CCE | 行为组合覆盖 | 指数空间 | Hartley 熵 |
| DPE | 行为模式多样性 | 聚类分布 | Shannon 熵 |
| BCE | 行为类别多样性 | 分类分布 | Shannon 熵 |

---

## 7 文献引用（对应 bib 文件）
详见 accompanying .bib 文件。


